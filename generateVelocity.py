#import random


def generateVelocityField(dims):
    ## center for circle
    midx = dims[0]/2.0
    midy = dims[1]/2.0
    width = dims[0] / 7.0
    epsilon = ra
    for j in range(dims[1]):
        for i in range(dims[0]):
            pass





generateVelocityField([30,30])
